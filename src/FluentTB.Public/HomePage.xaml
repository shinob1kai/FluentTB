<Page x:Class="FluentTB.Public.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      d:DesignHeight="1200" d:DesignWidth="600"
      Title="HomePage"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      d:Background="White">

    <StackPanel MaxWidth="1000" Margin="16">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="{DynamicResource TitleText}" FontSize="24" FontWeight="Medium" VerticalAlignment="Center" />
            <TextBlock x:Name="VersionTextBlock" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" Margin="10,6,0,0" Opacity="0.5" />
        </StackPanel>

        <TextBlock Text="{DynamicResource HomeTitle}" FontSize="24" FontWeight="SemiBold" Margin="0,36,0,12" />
        <StackPanel Orientation="Horizontal">
            <ui:Button x:Name="ViewUpdatesButton" Appearance="Transparent" Padding="0,0,16,0" BorderBrush="Transparent" Margin="0,0,16,16" Click="ViewUpdates_Click">
                <StackPanel Orientation="Horizontal">
                    <Border CornerRadius="8" ClipToBounds="True" Width="200" Height="98" Margin="0,0,16,0">
                        <Border.Background>
                            <ImageBrush ImageSource="/Resources/FluentFlyoutHero-NoTitle.png" Stretch="UniformToFill" />
                        </Border.Background>
                    </Border>
                    <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                        <ui:TextBlock Text="{DynamicResource UpdateToastButton}" FontWeight="SemiBold" />
                        <ui:TextBlock Text="{DynamicResource HomeUpdateDescription}" Opacity="0.5" FontSize="12" />
                    </StackPanel>
                </StackPanel>
            </ui:Button>

            <!--Update status indicator-->
            <ui:Button x:Name="CheckButton" Appearance="Transparent" Padding="8" BorderBrush="Transparent" Margin="0,0,16,16" Click="CheckForUpdates_Click">
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon x:Name="UpdateStatusIcon" Symbol="ArrowSync24" FontSize="24" Margin="0,0,12,0">
                    </ui:SymbolIcon>
                    <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                        <ui:TextBlock x:Name="UpdateStatusText" FontWeight="SemiBold" MaxWidth="180" TextWrapping="Wrap">
                        </ui:TextBlock>
                        <ui:TextBlock x:Name="LastCheckedText" Opacity="0.5" FontSize="12" />
                    </StackPanel>
                </StackPanel>
            </ui:Button>

        </StackPanel>

        <TextBlock Grid.Row="6" Text="{DynamicResource HomeDashboardTitle}" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,6" />
        <Grid Margin="-4" x:Name="DashboardGrid"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
            <ui:CardAction Grid.Row="0" Grid.Column="0" Margin="4" Padding="12,20" IsChevronVisible="True" Click="Taskbar_Click">
                <ui:CardAction.Icon>
                    <ui:SymbolIcon Symbol="TextboxAlignBottom24" FontSize="30" Filled="False" />
                </ui:CardAction.Icon>
                <StackPanel>
                    <ui:TextBlock FontWeight="Medium" Text="{DynamicResource FtbTaskbarTitle}" />
                    <ui:TextBlock x:Name="TaskbarStatusText" Appearance="Secondary" Text="{DynamicResource Enabled}"/>
                </StackPanel>
            </ui:CardAction>







            <ui:CardAction Grid.Row="0" Grid.Column="1" Margin="4" Padding="12,20" IsChevronVisible="True" Click="LockKeys_Click">
                <ui:CardAction.Icon>
                    <ui:SymbolIcon Symbol="LockMultiple24" FontSize="30" Filled="False" />
                </ui:CardAction.Icon>
                <StackPanel>
                    <ui:TextBlock FontWeight="Medium" Text="{DynamicResource LockKeysCustomizationTitle}" />
                    <ui:TextBlock Appearance="Secondary" x:Name="LockStatusText">
                        <ui:TextBlock.Style>
                            <Style TargetType="ui:TextBlock">
                                <Setter Property="Opacity" Value="1" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Enabled}" Value="False">
                                        <Setter Property="Opacity" Value="0.5" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ui:TextBlock.Style>
                    </ui:TextBlock>
                </StackPanel>
            </ui:CardAction>




        </Grid>

    </StackPanel>
</Page>
